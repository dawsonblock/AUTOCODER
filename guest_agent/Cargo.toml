[package]
name = "guest_agent"
version = "19.0.0"
edition = "2021"

[target.'cfg(target_os = "linux")'.dependencies]
vsock = "0.5.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"

